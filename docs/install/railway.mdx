---
summary: "Deploy OpenClaw on Railway from this repository"
read_when:
  - Deploying OpenClaw to Railway
  - You want a Railway deploy with browser-based Control UI
title: "Railway"
---

# Railway

Deploy OpenClaw on Railway from this repository and access it through the web Control UI.
This is the easiest "no terminal on the server" path: Railway runs the Gateway for you.

## Quick checklist (new users)

1. Create a Railway service from this GitHub repository.
2. Keep the root `railway.toml` in place so Railway uses the repo's Dockerfile and healthcheck settings.
3. Add a **Volume** mounted at `/data`.
4. Set the required **Variables**:
   - `OPENCLAW_GATEWAY_PORT=8080`
   - `OPENCLAW_GATEWAY_TOKEN`
   - `OPENCLAW_STATE_DIR=/data/.openclaw`
   - `OPENCLAW_WORKSPACE_DIR=/data/workspace`
5. Enable **HTTP Proxy** on port `8080`.
6. Open `https://<your-railway-domain>/openclaw` and connect using the configured shared secret.

## Deploy

After deploy, find your public URL in **Railway → your service → Settings → Domains**.

Railway will either:

- give you a generated domain (often `https://<something>.up.railway.app`), or
- use your custom domain if you attached one.

Then open:

- `https://<your-railway-domain>/openclaw` — Control UI

## What you get

- Hosted OpenClaw Gateway + Control UI
- Persistent storage via Railway Volume (`/data`) so `openclaw.json`,
  per-agent `auth-profiles.json`, channel/provider state, sessions, and
  workspace survive redeploys
- A Railway-friendly config-as-code entry point in `railway.toml`

## Required Railway settings

### Public Networking

Enable **HTTP Proxy** for the service.

- Port: `8080`

### Volume (required)

Attach a volume mounted at:

- `/data`

### Variables

Set these variables on the service:

- `OPENCLAW_GATEWAY_PORT=8080` (required — must match the port in Public Networking)
- `OPENCLAW_GATEWAY_TOKEN` (required; treat as an admin secret)
- `OPENCLAW_STATE_DIR=/data/.openclaw` (recommended)
- `OPENCLAW_WORKSPACE_DIR=/data/workspace` (recommended)

## About PostgreSQL

OpenClaw itself stores its runtime state on disk under the state directory and
does not use PostgreSQL as its core persistence layer. If you want a database
for an MDH companion service, add a separate Railway PostgreSQL service and
point that companion layer at it. Keep OpenClaw's own state on the mounted
volume.

## Connect a channel

Use the Control UI at `/openclaw` or run `openclaw onboard` via Railway's shell for channel setup instructions:

- [Telegram](/channels/telegram) (fastest — just a bot token)
- [Discord](/channels/discord)
- [All channels](/channels)

## Backups & migration

Export your state, config, auth profiles, and workspace:

```bash
openclaw backup create
```

This creates a portable backup archive with OpenClaw state plus any configured
workspace. See [Backup](/cli/backup) for details.

## Next steps

- Set up messaging channels: [Channels](/channels)
- Configure the Gateway: [Gateway configuration](/gateway/configuration)
- Keep OpenClaw up to date: [Updating](/install/updating)
